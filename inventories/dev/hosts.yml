---
# Development Inventory
# This inventory is for the development/testing environment

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
